UPDATE book SET age=25 WHERE author='Masumi';

/*
UPDATE句は、「対象テーブルのデータ（レコード）を更新するコマンド」です。
以下の例では、bookというテーブルにおいて、authorがMasumiのレコードに対して、ageを25に更新するという処理をしています。

更新対象のデータのみ更新する場合、UPDATE文はWHERE句とセットで用います。
*/